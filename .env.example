# GPU pod base URL for SDSS v2 endpoints.
# When unset, the /case page runs against mock data.
# Set to the vast.ai pod URL (e.g. https://xxx.vast.ai) or ngrok tunnel.
# VITE_GPU_POD_URL=https://your-gpu-pod.vast.ai
